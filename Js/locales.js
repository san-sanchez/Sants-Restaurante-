const ciudad = document.getElementById("ciudad");

const ubicacion = document.getElementById("ubi_local");

const horario = document.getElementById("hr_local");

const img = document.querySelectorAll("local_Img");

img.
